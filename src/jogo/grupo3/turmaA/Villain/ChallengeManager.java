class ChallengeManager {
    public boolean executeRandomChallenge(String challengeLevel) {
        // Simulando um desafio aleatório
        return Math.random() > 0.5; // Retorna verdadeiro ou falso aleatoriamente
    }
}
